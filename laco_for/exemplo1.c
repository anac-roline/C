#include <stdio.h>

int main(){
    for (int i = 1; i <= 5; i++){ // i é a variável de controle, iniciada em 1
        //Condição: enquanto i for menor ou igual a 5
        //Incremento: i aumenta 1 a cada iteração
        printf("%d\n", i);
    }
    return 0;
}